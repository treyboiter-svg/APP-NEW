# OverlineEdge v8 — Python 3.14 Windows 64-bit compatible
# ALL packages below have pre-built .whl files — zero C compiler needed
# Last verified: 2026-07-13

fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.28.1

# greenlet 3.2.3+ has a pre-built cp314-win_amd64 wheel on PyPI
# 3.1.1 does NOT — it requires MSVC compiler (fails on clean Windows)
greenlet>=3.2.3

# playwright depends on greenlet; pin to a version that resolves to greenlet>=3.2.3
playwright>=1.50.0

pandas==3.0.4
numpy==2.5.1
openpyxl==3.1.5
pytz==2024.2
python-multipart==0.0.20
